-- ============================================
-- 0008_birth_date_optional.sql
-- Дата рождения перестаёт быть обязательной — по решению школы это
-- личные данные, которые не хотят собирать при загрузке списков.
-- Защита от однофамильцев внутри класса теперь работает только по ФИО
-- (если дата всё же указана — используется как раньше, для точности).
-- ============================================

alter table students alter column birth_date drop not null;
